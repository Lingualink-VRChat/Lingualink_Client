<Window
    x:Class="lingualink_client.Views.OAuthLoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    Title="登录 - LinguaLink"
    Width="500"
    Height="700"
    MinWidth="400"
    MinHeight="500"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize"
    Background="{DynamicResource ApplicationBackgroundBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border
            Grid.Row="0"
            Padding="16,12"
            Background="{DynamicResource ControlFillColorDefaultBrush}">
            <Grid>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <ui:SymbolIcon FontSize="20" Symbol="Person24" />
                    <TextBlock
                        Margin="12,0,0,0"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="登录到 LinguaLink" />
                </StackPanel>
                <ui:Button
                    HorizontalAlignment="Right"
                    Appearance="Transparent"
                    Click="CloseButton_Click"
                    Icon="{ui:SymbolIcon Dismiss24}"
                    ToolTip="关闭" />
            </Grid>
        </Border>

        <!-- WebView2 容器 -->
        <Grid Grid.Row="1">
            <!-- WebView2 控件 -->
            <wv2:WebView2
                x:Name="LoginWebView"
                NavigationStarting="LoginWebView_NavigationStarting"
                NavigationCompleted="LoginWebView_NavigationCompleted"
                CoreWebView2InitializationCompleted="LoginWebView_CoreWebView2InitializationCompleted" />

            <!-- 加载指示器 -->
            <Grid
                x:Name="LoadingOverlay"
                Background="{DynamicResource ApplicationBackgroundBrush}"
                Visibility="Visible">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <ui:ProgressRing
                        Width="48"
                        Height="48"
                        IsIndeterminate="True" />
                    <TextBlock
                        Margin="0,16,0,0"
                        HorizontalAlignment="Center"
                        FontSize="14"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="正在加载登录页面..." />
                </StackPanel>
            </Grid>

            <!-- 错误提示 -->
            <Grid
                x:Name="ErrorOverlay"
                Background="{DynamicResource ApplicationBackgroundBrush}"
                Visibility="Collapsed">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MaxWidth="300">
                    <ui:SymbolIcon
                        FontSize="48"
                        Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                        Symbol="ErrorCircle24" />
                    <TextBlock
                        x:Name="ErrorMessageText"
                        Margin="0,16,0,0"
                        HorizontalAlignment="Center"
                        FontSize="14"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="加载失败"
                        TextAlignment="Center"
                        TextWrapping="Wrap" />
                    <ui:Button
                        Margin="0,16,0,0"
                        HorizontalAlignment="Center"
                        Appearance="Primary"
                        Click="RetryButton_Click"
                        Content="重试" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>



