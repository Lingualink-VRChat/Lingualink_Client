<Page
    x:Class="lingualink_client.Views.PeerAudioTranslationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:lingualink_client.ViewModels"
    Title="PeerAudioTranslationPage"
    d:DataContext="{d:DesignInstance Type=vm:PeerAudioTranslationWindowViewModel}"
    mc:Ignorable="d">

    <Grid Margin="{DynamicResource PageContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="16"
            Margin="0,0,0,12">
            <StackPanel>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Text="{Binding WindowTitle}"
                        FontSize="20"
                        FontWeight="SemiBold"
                        VerticalAlignment="Center" />

                    <ui:Button
                        Grid.Row="0"
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        Command="{Binding ToggleCommand}"
                        Content="{Binding ToggleButtonText}"
                        IsEnabled="{Binding IsFeatureToggleEnabled}"
                        Appearance="Primary"
                        Icon="{ui:SymbolIcon Mic24}" />

                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="0,6,0,0"
                        Text="{Binding Description}"
                        TextWrapping="Wrap"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </Grid>

                <Border
                    Margin="0,12,0,14"
                    Padding="16"
                    Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Visibility="{Binding IsMemberOnlyNoticeVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <ui:SymbolIcon
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                            FontSize="18"
                            Symbol="Info24" />

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock
                                FontSize="14"
                                FontWeight="SemiBold"
                                Text="{Binding MemberOnlyNoticeTitle}" />
                        </StackPanel>

                        <TextBlock
                            Grid.Column="4"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            FontSize="11"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{Binding MemberOnlyNoticeMessage}"
                            TextAlignment="Right"
                            TextWrapping="Wrap" />
                    </Grid>
                </Border>

                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center"
                        FontWeight="SemiBold"
                        Text="{Binding CaptureTargetLabel}" />

                    <ComboBox
                        Grid.Column="1"
                        DisplayMemberPath="DisplayName"
                        IsEnabled="{Binding IsCaptureTargetSelectionEnabled}"
                        ItemsSource="{Binding CaptureTargets}"
                        SelectedItem="{Binding SelectedCaptureTarget}" />

                    <ui:Button
                        Grid.Column="2"
                        Margin="8,0,0,0"
                        Command="{Binding RefreshCaptureTargetsCommand}"
                        Content="{Binding RefreshProcessesLabel}"
                        Appearance="Secondary"
                        Icon="{ui:SymbolIcon ArrowClockwise24}" />
                </Grid>

                <TextBlock
                    Margin="0,16,0,8"
                    Text="{Binding TargetLanguagesLabel}"
                    FontWeight="SemiBold" />

                <ItemsControl ItemsSource="{Binding PeerTargetLanguages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:PeerAudioLanguageItemViewModel}">
                            <Border
                                Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10,8"
                                Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        Text="{Binding Label}"
                                        VerticalAlignment="Center" />
                                    <ComboBox
                                        Grid.Column="1"
                                        DisplayMemberPath="DisplayName"
                                        ItemsSource="{Binding AvailableLanguages}"
                                        SelectedItem="{Binding SelectedDisplayLanguage}"
                                        IsEnabled="{Binding DataContext.IsPeerTargetLanguageSelectionEnabled, RelativeSource={RelativeSource AncestorType=Page}}" />
                                    <ui:Button
                                        Grid.Column="2"
                                        Margin="8,0,0,0"
                                        Command="{Binding RemoveCommand}"
                                        Appearance="Secondary"
                                        Icon="{ui:SymbolIcon Delete24}"
                                        ToolTip="{Binding RemoveLabel}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ui:Button
                    HorizontalAlignment="Left"
                    Command="{Binding AddPeerTargetLanguageCommand}"
                    Content="{Binding AddLanguageLabel}"
                    Appearance="Secondary"
                    Icon="{ui:SymbolIcon Add24}" />
            </StackPanel>
        </Border>

        <Border
            Grid.Row="1"
            Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12"
            Margin="0,0,0,12">
            <TextBlock
                Text="{Binding StatusText}"
                TextWrapping="Wrap"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </Border>

        <Border
            Grid.Row="2"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock
                            Text="{Binding EmbeddedResultLabel}"
                            FontWeight="SemiBold" />
                        <TextBlock
                            Margin="0,3,16,0"
                            Text="{Binding SteamVrWristHintMessage}"
                            TextWrapping="Wrap"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>

                    <StackPanel
                        Grid.Column="1"
                        Margin="12,0,12,0"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,8,0"
                            Text="{Binding SeparateWindowLabel}"
                            VerticalAlignment="Center" />
                        <ui:ToggleSwitch
                            VerticalAlignment="Center"
                            IsEnabled="{Binding IsFeatureToggleEnabled}"
                            IsChecked="{Binding ShowResultsInSeparateWindow, Mode=TwoWay}" />
                    </StackPanel>

                    <ui:Button
                        Grid.Column="2"
                        Command="{Binding ClearCommand}"
                        Content="{Binding ClearLabel}"
                        Appearance="Secondary"
                        Icon="{ui:SymbolIcon Delete24}" />
                </Grid>

                <ScrollViewer
                    x:Name="MessagesScrollViewer"
                    Grid.Row="1"
                    Visibility="{Binding IsEmbeddedResultVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                    VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Messages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:PeerAudioChatMessage}">
                                <Border
                                    HorizontalAlignment="Stretch"
                                    Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                    BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="12"
                                    Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock
                                            Grid.Row="0"
                                            HorizontalAlignment="Right"
                                            Text="{Binding TimestampText}"
                                            FontSize="11"
                                            Foreground="{DynamicResource TextFillColorTertiaryBrush}" />

                                        <TextBlock
                                            Grid.Row="1"
                                            Margin="0,6,0,0"
                                            Text="{Binding Text}"
                                            TextWrapping="Wrap"
                                            FontSize="14" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Text="{Binding SeparateWindowResultNotice}"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Visibility="{Binding ShowResultsInSeparateWindow, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Border>
    </Grid>
</Page>
