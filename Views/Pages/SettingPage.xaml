<Page
    x:Class="lingualink_client.Views.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:lingualink_client.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:lingualink_client.ViewModels"
    Title="SettingPage"
    d:DataContext="{d:DesignInstance Type=vm:SettingPageViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="{DynamicResource PageContentMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Page Title -->
            <TextBlock
                Grid.Row="0"
                Text="{Binding PageTitle}"
                Style="{DynamicResource PageTitleTextBlockStyle}" />

            <!-- Interface Language Section -->
            <StackPanel Grid.Row="1" Margin="0,0,0,30">
                <Label Content="{Binding InterfaceLanguage}" FontWeight="Bold" FontSize="14" />
                <ComboBox
                    x:Name="LanguageComboBox"
                    VerticalAlignment="Stretch"
                    SelectionChanged="OnLanguageChanged"
                    Margin="0,5,0,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <ui:TextBlock Text="{Binding NativeName}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock
                    Text="{Binding LanguageHint}"
                    TextWrapping="Wrap"
                    Margin="0,5,0,0"
                    FontStyle="Italic"
                    Foreground="Gray"
                    FontSize="11" />
            </StackPanel>

            <!-- Update Section -->
            <StackPanel Grid.Row="2" Margin="0,0,0,30">
                <Label Content="{Binding UpdateSectionTitle}" FontWeight="Bold" FontSize="14" />

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding CurrentVersionLabel}" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="1" Margin="12,0,0,0" Text="{Binding CurrentVersion}" />
                </Grid>

                <Grid Margin="0,5,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding LatestVersionLabel}" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="1" Margin="12,0,0,0" Text="{Binding LatestVersion}" />
                </Grid>

                <ui:Button
                    Margin="0,12,0,0"
                    Command="{Binding CheckForUpdateCommand}"
                    Content="{Binding CheckForUpdatesLabel}" />

                <StackPanel
                    Margin="0,12,0,0"
                    Visibility="{Binding HasUpdate, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ui:Button
                        Command="{Binding DownloadAndUpdateCommand}"
                        Content="{Binding DownloadAndUpdateLabel}" />

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0" VerticalAlignment="Center" Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock
                            Text="{Binding DownloadProgressLabel}"
                            VerticalAlignment="Center"
                            Margin="0,0,12,0" />
                        <ProgressBar
                            Width="200"
                            Height="16"
                            Maximum="100"
                            Value="{Binding DownloadProgress}" />
                    </StackPanel>

                    <Border
                        Margin="0,12,0,0"
                        BorderBrush="#FFE0E0E0"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="10"
                        Visibility="{Binding HasUpdate, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="120">
                            <TextBlock Text="{Binding UpdateNotes}" TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>

